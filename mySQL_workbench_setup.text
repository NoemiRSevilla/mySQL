USE twitter;

-- INSERT
INSERT INTO tweets (tweet, user_id, created_at, updated_at)
VALUES ("hello", 1, NOW(), NOW());

-- UPDATE
UPDATE users SET
first_name = "Kobe"
WHERE id = 1;

-- DELETE
DELETE FROM tweets
WHERE user_id = 14;

SELECT * FROM tweets